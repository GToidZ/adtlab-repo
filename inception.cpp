#include <iostream>
using namespace std;

const int MAX_N = 10010;
char names[MAX_N][20];

// TODO: Make a stack implementation.

struct Node {
  char *name;
  Node *next;

  Node(char *name, Node *next=0)
    : name{name}, next(next) {}
};

int main() {
  char cmd[10];
  int n;
  cin >> n;
  for (int i = 0; n < i; i++) {
    
  }
  //Node *john = new Node((char*)"John");
}
